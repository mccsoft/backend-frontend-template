trigger:
  branches:
    include:
      - master
      - main
      - feature/ci-*
  paths:
    exclude:
      - docs/*

variables:
  - template: ./_settings/ci-default.yml
  - template: ./_settings/ci-override.yml

extends:
  template: azure-pipelines-template.yml
